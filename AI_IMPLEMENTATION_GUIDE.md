# PlayEAT AI 구현 시작 가이드

이 문서는 Codex·Claude 등 구현 도구가 저장소에서 작업을 시작할 때 적용할 공통 진입점이다. 세부 요구사항을 복제하지 않고 기준 문서의 읽기 순서와 작업 절차만 정의한다.

현재 구현 기준선은 `PRD-BASELINE-1.0`이다. 추가 기능을 제안하거나 범위를 넓히지 않고 확정 문서를 그대로 구현한다.

## 1. 필수 참조 순서

1. 교과목 공식 평가기준 P1-1~P1-4와 필수과제 M0~M9
2. `PRD_개정본.md`
3. `design.md`
4. `API_설계서.md`
5. `encore_project.xlsx`의 `정의서_new` 탭과 승인된 PRD DB 변경
6. `함수명_코드_컨벤션.md`
7. `깃허브생성가이드.md`
8. `PRD_결정사항.md`와 `PRD_변경추적.md`

충돌하면 위 순서를 적용한다. `design.md`는 공통 시각 규칙을, PRD는 기능·데이터·업무 규칙을 담당한다.

## 2. 구현 시작 전 확인

- 담당 페이지와 연결되는 PRD 요구사항 ID·API·DB 테이블을 찾는다.
- 필요한 API가 `API_설계서.md`에 없으면 화면에서 임의 호출 규격을 만들지 않는다.
- 필요한 데이터가 DB 정의에 없으면 임시 JSON이나 별도 컬럼을 만들기 전에 변경사항으로 기록한다.
- 공통 layout·navbar·components·styles를 먼저 확인하고 같은 역할의 UI를 새로 만들지 않는다.
- 구현 범위가 P0인지 P1·P2인지 확인한다. M0~M9와 P1-1~P1-4가 다른 기능보다 우선한다.

## 3. 페이지 구현 계약

- Streamlit은 화면·입력·표시 상태를 담당하고 핵심 업무 규칙은 FastAPI가 수행한다.
- 핵심 데이터 요청은 FastAPI를 통과해 `api_request_logs`에 남아야 한다.
- 생성·실행 요청은 `X-Idempotency-Key`를 사용한다.
- Loading·Empty·Success·Error를 서로 구분하고 실패한 결과를 성공처럼 표시하지 않는다.
- 401·403·404·409·422·5xx는 표준 에러 응답의 `code`, `message`, `details`, `request_id`를 사용한다.
- 페이지별 HEX·폰트·간격·버튼·카드 스타일을 추가하지 않고 `design.md`의 토큰과 공통 컴포넌트를 사용한다.
- PRD에 없는 기능·화면·데이터·AI 추론을 임의로 추가하지 않는다.

## 4. 작업 완료 보고 형식

각 작업의 완료 보고에는 다음을 포함한다.

1. 구현한 PRD 요구사항과 평가기준
2. 변경한 파일과 핵심 동작
3. 연결 API와 DB 테이블
4. Loading·Empty·Error·권한·중복 요청 검증 결과
5. 실행한 테스트 또는 수동 시나리오와 결과
6. 남은 불확실성·결정 필요사항

## 5. 금지사항

- Streamlit에서 Supabase 핵심 업무 테이블을 직접 조회·수정하여 FastAPI 로그를 우회하지 않는다.
- Gemini가 DB에 없는 식당·메뉴·가격·통계·원인을 만들게 하지 않는다.
- 요청·응답 본문, 비밀번호, 토큰, API Key를 API 로그에 저장하지 않는다.
- 원본 로그를 정제 결과로 덮어쓰지 않는다.
- 추천 제품 지표와 API 운영 지표를 같은 의미로 합산하지 않는다.
- 평가 기준을 통과시키기 위해 고정 응답이나 시연 전용 가짜 데이터를 제품 경로에 넣지 않는다.

## 6. 구현 완료 기준

- 코드·OpenAPI·DB·화면이 기준 문서와 일치한다.
- 배포 Streamlit 요청이 FastAPI를 거쳐 Supabase 원본 로그에 저장된다.
- 정제·통계 3종·LLM 요약·근거·품질평가·개선 실험 결과를 대시보드에서 추적한다.
- 담당 페이지가 `design.md`의 공통 구조와 상태 표현을 사용한다.
- 관련 P1 기준과 M 단계의 완료 증거를 남긴다.
