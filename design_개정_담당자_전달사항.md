# `design.md` 개정 담당자 전달사항

## 1. 문서의 역할

- `design.md`는 모든 페이지가 공유하는 디자인 시스템을 정의합니다.
- 페이지별 기능, 데이터, API, 업무 규칙은 PRD가 담당합니다.
- `design.md`에는 공통 레이아웃, 디자인 토큰, 컴포넌트, 상태 및 상호작용 표현 규칙을 작성합니다.
- AI가 색상, 간격, 컴포넌트 형태를 임의로 결정하지 않도록 정확한 값과 사용 조건을 명시합니다.

## 2. 필수 디자인 토큰

다음 디자인 토큰의 값과 사용 조건을 정의합니다.

- 색상: `primary`, `secondary`, `accent`, `background`, `surface`, `border`, `text`, `muted text`
- 상태 색상: `success`, `warning`, `error`, `info`
- 차트용 색상 팔레트와 각 색상의 의미
- 폰트 종류
- 제목, 본문, 캡션, 수치의 크기와 굵기
- 간격 단위와 페이지, 섹션, 카드, 컴포넌트 사이의 여백
- 테두리 두께, 모서리 반경, 그림자
- 아이콘 크기와 사용 규칙
- 화면 최대 너비와 기본 그리드

“적당히”, “일관되게” 같은 추상적인 표현은 사용하지 않습니다. 색상 코드와 `px`, `rem` 등 실제 구현에 사용할 수 있는 값으로 작성합니다.

## 3. 공통 화면 구조

다음 영역의 공통 배치와 계층 구조를 정의합니다.

- 사이드바 또는 상단 내비게이션
- 페이지 헤더와 제목 영역
- 필터, 검색, 기간 선택 영역
- KPI 카드 영역
- 표, 차트, 로그 목록 영역
- 상세 패널 또는 상세 페이지
- 설정 화면
- 오류 화면
- 공통 푸터 또는 상태 표시 영역

메인, 상세, 입력, 설정, 에러 화면이 동일한 정보 계층과 정렬 규칙을 따르도록 정의합니다.

## 4. 공통 컴포넌트

다음 컴포넌트의 기본 형태와 상태별 모습을 정의합니다.

- 기본, 보조, 위험 버튼
- 텍스트 입력, 검색창, 선택 상자, 날짜 및 기간 선택
- 체크박스, 토글, 라디오 버튼
- 탭, 배지, 툴팁
- KPI 카드와 일반 카드
- 데이터 테이블과 페이지 이동
- 필터 및 적용·초기화 버튼
- 차트와 범례
- 로그 행과 로그 상세 표시
- 모달 또는 확인 창
- 알림 메시지
- 로딩 표시와 스켈레톤
- 빈 데이터 화면
- 성공, 경고, 오류 상태

각 컴포넌트에는 구현 가능한 범위에서 `default`, `hover`, `focus`, `active`, `selected`, `disabled`, `loading`, `error` 상태를 정의합니다.

## 5. 사용자 액션에 대한 시각적 반응

교과목 공식 평가기준 P1-2를 충족할 수 있도록 다음 사용자 액션에 대한 화면 반응을 명확히 정의합니다.

- 클릭
- 호버
- 키보드 포커스
- 드래그
- 스크롤
- 필터 적용과 초기화
- 검색
- 정렬
- 페이지 이동
- 데이터 새로고침
- API 요청 중
- 요청 성공과 실패

예를 들어 데이터 새로고침 시 다음 사항을 구체적으로 정의합니다.

- 로딩 표시가 나타나는 위치와 형태
- 새로고침 중 비활성화되는 요소
- 완료 후 마지막 갱신 시각을 표시하는 방법
- 실패 시 오류 메시지와 재시도 버튼을 표시하는 방법

## 6. 대시보드 및 로그 화면 규칙

- 차트 제목, 축, 단위, 범례, 툴팁의 위치와 형식
- 정상, 경고, 오류 로그의 색상과 아이콘
- 색상만으로 상태를 구분하지 않고 텍스트나 아이콘을 함께 사용하는 규칙
- 긴 로그와 긴 LLM 응답의 줄바꿈, 말줄임, 펼치기 규칙
- 표의 고정 헤더, 정렬, 필터, 빈 값 표시
- 데이터가 없는 상태와 데이터 수집에 실패한 상태의 구분
- 마지막 갱신 시각과 새로고침 상태 표시
- 실시간 또는 주기적 갱신 중 화면이 흔들리지 않도록 레이아웃을 유지하는 규칙

## 7. 접근성과 반응형 규칙

- 본문과 배경 사이에 충분한 명도 대비를 적용합니다.
- 키보드 포커스가 명확하게 보이도록 표시합니다.
- 상태 정보를 색상에만 의존하지 않습니다.
- 버튼과 입력 요소의 최소 클릭 영역을 정의합니다.
- 좁은 화면에서 표와 차트를 표시하는 방법을 정의합니다.
- 긴 텍스트와 화면 확대 상태에서도 주요 기능을 사용할 수 있도록 설계합니다.

## 8. Streamlit 구현 가능성

이번 결과물은 Streamlit 대시보드가 중심입니다. 기본 Streamlit 컴포넌트 또는 실제 구현 가능한 CSS 범위 안에서 디자인 규칙을 작성합니다.

- 구현이 어렵거나 불안정한 애니메이션을 필수 규칙으로 지정하지 않습니다.
- 복잡한 드래그 상호작용을 필수 기능으로 지정하지 않습니다.
- 과도한 커스텀 프런트엔드 효과를 필수 규칙으로 지정하지 않습니다.
- Streamlit 기본 컴포넌트를 사용하는 항목과 별도 컴포넌트 또는 CSS가 필요한 항목을 가능한 범위에서 표시합니다.

## 9. AI 구현 통일성을 위한 금지사항

AI가 페이지별로 임의의 디자인을 추가하지 못하도록 다음 금지사항을 명시합니다.

- 정의되지 않은 색상, 폰트, 그림자 사용 금지
- 페이지마다 다른 버튼 형태 사용 금지
- 동일한 상태에 서로 다른 색상 사용 금지
- 장식 목적의 그라데이션, 이모지, 애니메이션 임의 추가 금지
- 동일한 의미의 컴포넌트를 새로운 형태로 중복 생성하는 행위 금지
- 차트마다 임의의 색상 팔레트 사용 금지
- PRD에 없는 기능이나 화면을 디자인 판단으로 추가하는 행위 금지

## 10. 공식 평가기준 P1-2 확인

최종 `design.md`가 다음 P1-2 기준을 직접 뒷받침하는지 확인합니다.

- 메인, 상세, 입력, 설정, 에러 화면의 일관된 설계
- 논리적인 정보 계층구조
- 버튼, 입력폼, 테이블, 차트의 통일된 스타일
- 색상, 폰트, 여백의 일관된 디자인 시스템
- 클릭, 호버, 드래그, 스크롤, 필터, 새로고침 등 사용자 액션별 시스템 반응

## 11. AI 구현 후 확인 체크리스트

각 페이지를 구현한 후 다음 항목을 확인합니다.

- [ ] 지정된 디자인 토큰만 사용했다.
- [ ] 공통 레이아웃과 정보 계층을 따랐다.
- [ ] 기존 공통 컴포넌트를 재사용했다.
- [ ] 기본, 호버, 포커스, 비활성화 상태가 일관된다.
- [ ] 로딩, 빈 데이터, 성공, 경고, 오류 상태를 구현했다.
- [ ] 필터, 검색, 정렬, 새로고침의 반응을 구현했다.
- [ ] 차트의 색상, 축, 단위, 범례, 툴팁이 규칙과 일치한다.
- [ ] 긴 로그와 LLM 응답이 레이아웃을 깨뜨리지 않는다.
- [ ] 색상 외에도 텍스트나 아이콘으로 상태를 구분한다.
- [ ] 키보드 포커스와 명도 대비 기준을 만족한다.
- [ ] Streamlit과 배포 환경에서 실제 구현 가능한 디자인이다.
- [ ] PRD에 없는 기능이나 디자인을 임의로 추가하지 않았다.
- [ ] P1-2의 모든 평가 항목을 증명할 수 있다.
