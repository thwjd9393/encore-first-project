# PlayEAT 팀 공통 UI 개발 가이드

> 로그인·회원가입·메인·마이페이지·관리자를 각각 구현해도 같은 공통 파일을 사용하여 하나의 서비스처럼 보이게 만드는 구현 기준입니다.
>
> 기준 문서: `design.md`. 기존 확정 디자인 값과 Streamlit 공통 구조를 유지하고, 구현 규칙 중심으로 재구성했습니다.

## 1. 목적과 적용 범위

- 적용 화면: 로그인, 회원가입, 메인 맛집 추천, 마이페이지, 프로필 수정, 비밀번호 변경, 관리자 검색정보분석·식당정보·사용자피드백 및 각 화면의 상태 표현.
- 팀에서 확정한 UI 화면을 기능과 정보 구성의 기준으로 사용합니다. 이 문서는 색상·크기·정렬·컴포넌트·상태 표현을 통일하는 기준입니다.
- 기존 화면의 기능, 입력 순서, 정보 순서, 고유 배치를 최대한 유지합니다. 별도 재추천 버튼, 식당 상세, 거절 이유 화면 등 새로운 기능이나 화면을 이 문서를 근거로 추가하지 않습니다.
- 인증, 권한, API, 데이터 처리, 화면 이동 및 조건 보존 정책은 별도 기능 명세에서 관리합니다. 페이지는 그 결과를 공통 UI로 표시합니다.
- 원본의 확정값은 유지합니다. 원본에서 권장한 보조색·줄 높이·입력창 보조 스타일·카드 패딩·반응형 기준은 아래에서도 **권장 기본값**으로 구분합니다. 적용값은 공통 담당자가 관리하며 페이지별로 다르게 선택하지 않습니다.
- 이번에 보완한 표·차트 및 상태 표현 규칙은 공통 구현 규칙입니다. 원본에 없는 색상이나 크기를 기존 확정값처럼 추가하지 않습니다.

## 2. 디자인 토큰

### 2.1 색상

| 토큰 | 값 | 역할 | 구분 |
| --- | --- | --- | --- |
| `--color-primary` | `#FF7A00` | 주요 버튼, 활성 메뉴, 강조 | 확정 |
| `--color-secondary` | `#FFB368` | 보조 강조, 배지, 장식 | 확정 |
| `--color-background` | `#FFF9F4` | 페이지 배경 | 확정 |
| `--color-text` | `#333333` | 제목과 본문 | 확정 |
| `--color-surface` | `#FFFFFF` | 카드, 입력창, 보조 버튼 | 권장 기본값 |
| `--color-muted` | `#666666` | 설명, 날짜, 보조 텍스트 | 권장 기본값 |
| `--color-border` | `#DDDDDD` | 입력창·카드·보조 버튼 테두리 | 권장 기본값 |
| `--color-danger` | `#C62828` | 위험 동작, 오류 | 권장 기본값 |
| `--color-success` | `#2E7D32` | 만족·성공 상태, 완료 배지 | 권장 기본값 |
| `--color-warning` | `#E08A00` | 보통·주의 상태, 경고 배지 | 권장 기본값 |
| `--color-info` | `#2F6FED` | 안내성 정보, 중립 배지 | 권장 기본값 |

Secondary 색상과 Secondary 버튼은 다른 개념입니다. Secondary 버튼은 흰색 배경과 회색 테두리를 사용합니다. 오류는 색상과 설명을 함께 표시합니다.

### 2.2 공통 토큰 선언

아래 선언은 원본 토큰을 그대로 유지한 공통 기준입니다. `styles.css`에서 한 번 정의하고 페이지에서는 참조합니다.

```css
:root {
  --color-primary: #FF7A00;
  --color-secondary: #FFB368;
  --color-background: #FFF9F4;
  --color-text: #333333;
  --color-surface: #FFFFFF;
  --color-muted: #666666;
  --color-border: #DDDDDD;
  --color-danger: #C62828;
  --color-success: #2E7D32;
  --color-warning: #E08A00;
  --color-info: #2F6FED;

  --icon-size-sm: 16px;
  --icon-size-md: 20px;
  --icon-size-lg: 24px;
  --min-tap-target: 44px;

  --font-family: 'Pretendard', -apple-system, BlinkMacSystemFont,
    'Segoe UI', sans-serif;
  --font-page-title: 40px;
  --font-section-title: 24px;
  --font-card-title: 20px;
  --font-card-title-large: 22px;
  --font-body: 16px;
  --font-caption: 14px;

  --layout-max-width: 1320px;
  --content-max-width: var(--layout-max-width);
  --content-login: 1200px;
  --content-signup: 600px;
  --content-main: 1320px;
  --content-mypage: 1000px;
  --content-dashboard: 1320px;
  --page-padding-x: 44px;
  --page-padding-top: 28px;
  --page-padding-bottom: 35px;
  --navbar-min-height: 90px;
  --radius-navbar: 18px;
  --shadow-common: 0 4px 16px rgb(51 51 51 / 6%);
  --input-height: 52px;
  --button-height: 52px;
  --radius-control: 10px;
  --radius-card: 20px;

  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
}
```

토큰 선언은 완성된 위젯 스타일이 아닙니다. 실제 Streamlit 위젯에 연결하는 작업은 공통 담당자가 수행합니다. 공통 그림자가 필요할 때는 `--shadow-common`을 사용하며 카드마다 다른 그림자를 만들지 않습니다.

아이콘은 `--icon-size-sm/md/lg` 세 단계만 사용하고 페이지마다 다른 두께·스타일의 아이콘을 섞지 않습니다. 만족(success)·보통(warning)·불만족(danger) 등 상태를 나타내는 배지·도넛차트·KPI 카드는 반드시 `--color-success`/`--color-warning`/`--color-danger` 세 토큰만 사용하며 같은 의미에 임의의 색상(예: 초록·주황·빨강의 다른 채도)을 새로 만들지 않습니다. 클릭 가능한 모든 버튼·입력·아이콘 버튼·페이지네이션 요소는 `--min-tap-target`(44px) 이상의 클릭 영역을 확보합니다.

## 3. 레이아웃과 콘텐츠 너비

| 항목 | 공통 기준 |
| --- | --- |
| 바깥 레이아웃 | 최대 1320px, 가운데 정렬 |
| 좌우 최소 여백 | 각각 44px |
| 위 / 아래 여백 | Navbar 위 28px / 마지막 콘텐츠 아래 35px |
| Navbar와 본문 사이 | 32px |
| Navbar | 최소 높이 90px, radius 18px |
| 전체 페이지 높이 | 콘텐츠에 따라 자동 확장 |

| 페이지 | 원본 데스크톱 범위 | 사용할 기본 토큰 |
| --- | --- | --- |
| 로그인 | 1100~1200px | `--content-login: 1200px` |
| 회원가입 | 520~600px | `--content-signup: 600px` |
| 메인 | 최대 1320px | `--content-main: 1320px` |
| 마이페이지·프로필 수정·비밀번호 변경 | 900~1000px | `--content-mypage: 1000px` |
| 관리자 | 최대 1320px | `--content-dashboard: 1320px` |

- 바깥 너비는 `min(1320px, 화면 너비 - 88px)`입니다. 1320px 안에 44px 여백을 다시 넣지 않습니다.
- Navbar는 바깥 컨테이너 기준으로 정렬하고 본문만 페이지별 최대 너비로 가운데 정렬합니다.
- 페이지별 범위의 하한은 최소 너비가 아닙니다. 모바일에 `min-width: 1100px` 등을 적용하지 않습니다.
- 로그인 너비는 전체 콘텐츠 기준입니다. 내부 로그인 폼을 1200px로 늘리지 않습니다.
- 기존 컬럼과 카드 배치는 유지합니다. 페이지 CSS는 컬럼 수·배치·정렬을 담당하고 색상·폰트·컨트롤 외형을 재정의하지 않습니다.
- Streamlit 기본 콘텐츠 패딩과 공통 여백의 중복 제거는 `layout.py`와 공통 스타일에서 처리합니다.

## 4. Navbar

모든 페이지가 `navbar.py`를 사용합니다. 로그인 전후의 구성이 달라도 같은 로고·배경·정렬·최소 높이·radius·패딩·그림자 기준을 공유합니다.

| 상황 | 구성 및 적용 |
| --- | --- |
| 로그인 전 | 로고 중심 단순 헤더. 로그인 후 전용 메뉴 숨김 |
| 일반 사용자 | PlayEAT / 맛집 추천 / 마이페이지 / 로그아웃 |
| 마이페이지 하위 화면 | 마이페이지를 활성 메뉴로 표시 |
| 관리자 | 공통 Navbar 외형 유지, 확정된 관리자 탐색 항목과 하위 메뉴 사용 |

- 최대 너비 1320px, 기본 최소 높이 90px, radius 18px을 적용합니다. 데스크톱에서는 90px을 유지하고, 좁은 화면에서 메뉴 줄바꿈이 필요한 경우에만 콘텐츠가 잘리거나 겹치지 않도록 높이 증가를 허용합니다. 로그인·회원가입·메인·마이페이지뿐 아니라 오류·성공·완료·빈 상태 등 모든 화면에 같은 기준을 적용합니다. 현재 화면 간 비교에서 회원가입 완료·오류 화면의 Navbar가 더 낮게 보이는 차이가 발견되었으므로, 공통 컴포넌트 적용 후 기본 높이를 반드시 90px로 맞춥니다.
- 원본 CSS 기준 내부 패딩은 24px, 배경은 Surface, 그림자는 `--shadow-common`입니다.
- 로고는 아이콘 높이 32px, 로고 텍스트 22px/700을 공통 기준으로 사용하며 페이지 유형과 관계없이 모든 화면에서 동일한 크기로 표시합니다. 현재 회원가입 화면 로고가 다른 화면보다 작게 표시되는 불일치가 발견되었으므로, 로고는 페이지별로 별도 크기를 지정하지 않고 공통 컴포넌트의 크기를 그대로 사용합니다.
- 활성 메뉴는 Primary 색상과 밑줄을 함께 사용합니다. 원본 CSS의 밑줄 두께 2px, offset 8px을 유지하며, 이 두께와 색상은 모든 화면의 모든 활성 메뉴 인스턴스에서 예외 없이 동일해야 합니다. 화면마다 밑줄이 더 얇거나 진하게 보이는 차이가 있다면 공통 컴포넌트 쪽 값을 수정합니다.
- 활성 링크에는 현재 위치를 전달하는 접근성 정보를 제공합니다.
- 로그아웃은 동작 버튼이며 활성 페이지로 표시하지 않습니다.
- 관리자 등 기존 사이드바의 화면 고유 하위 메뉴는 유지합니다. 전역 메뉴를 페이지마다 중복 제작하지 않습니다.
- 관리자 사이드바는 페이지가 달라도 같은 폭(약 250px)·항목 높이(48px)·아이콘 크기(`--icon-size-md`)·radius(10px)를 사용합니다. 활성 메뉴 항목은 Primary 배경 + 흰색 글씨로 표시하고, 이 표시 방식은 검색정보분석·식당정보·사용자피드백 세 화면에서 동일해야 합니다. 사이드바 하단의 필터 그룹(음식별·가격대별·매뉴 특성·사용자 반응)도 같은 pill 버튼 스타일(선택 시 Primary 배경, 미선택 시 Surface 배경 + Border 테두리)을 공유합니다.
- 메뉴 표시와 활성 상태에 필요한 로그인 상태·역할·현재 화면은 전달받습니다. 인증·권한 판단을 Navbar 스타일 코드에 중복 구현하지 않습니다.

## 5. Typography

공통 파일에서 Pretendard Regular(400), SemiBold(600), Bold(700)를 로드합니다. `font-family` 선언만으로 폰트가 설치되지는 않습니다.

| 역할 | 크기 | 굵기 | 권장 줄 높이 |
| --- | --- | --- | --- |
| 페이지 제목 | 40px | 700 | 1.3 |
| 섹션 제목 | 24px | 700 | 1.4 |
| 카드 제목 | 기본 20px / 강조형 22px | 700 | 1.4 |
| 본문 | 16px | 400 | 1.6 |
| 입력 내용 | 16px | 400 | 1.5 |
| 버튼 | 16px | 600 | 1.5 |
| 보조 설명·날짜·오류 안내 | 14px | 400 | 1.5 |

- 입력 라벨은 본문 크기 16px을 공통 기본값으로 사용합니다. 별도 라벨 스타일이 필요하면 공통 변형으로 추가합니다.
- 같은 종류의 카드 제목은 같은 크기를 사용합니다. 22px을 페이지 담당자의 취향으로 선택하지 않습니다.
- 페이지 제목 40px/700 규칙은 프로필 수정·마이페이지 같은 일반 화면뿐 아니라 회원가입 완료·회원가입 실패·서버 오류·검색 결과 없음 등 안내형 화면의 제목에도 예외 없이 적용합니다. 현재 화면별로 제목 크기가 서로 다르게 보이는 불일치가 발견되었으므로, 안내형 화면이라는 이유로 제목을 축소하지 않습니다.
- 제목은 정보 계층에 맞는 h1/h2/h3 구조로 표현합니다.
- 브라우저 기본 제목 여백을 그대로 섞지 않고 공통 spacing을 적용합니다.
- 좁은 화면에서 페이지 제목을 32px로 조정하는 원본 권장 규칙은 공통 반응형에서 관리합니다.

## 6. Buttons

공통 크기: **높이 52px / 좌우 패딩 24px / radius 10px / 글씨 16px·600**.

| 종류 | 스타일 | 적용 예 |
| --- | --- | --- |
| Primary | Primary 배경 + Surface 글씨 | 로그인, 저장, 추천 실행 |
| Secondary | Surface 배경 + Border 테두리 + Text 글씨 | 취소, 이전, 수정 화면 진입 |
| Danger | Danger 배경 + Surface 글씨 | 확정 UI의 위험 삭제 동작 |

- 한 행동 영역의 주요 동작 하나를 Primary로 강조합니다.
- 같은 역할의 버튼은 같은 공통 변형을 사용합니다. 페이지에서 배경·높이·radius를 따로 지정하지 않습니다.
- 회원가입 폼의 버튼도 예외가 아닙니다. 현재 회원가입 화면의 버튼이 다른 화면보다 낮고 각지게 보이는 불일치가 발견되었으므로, 높이 52px·radius 10px을 다른 화면과 동일하게 적용합니다.
- 버튼의 전체 너비 사용 여부와 배치는 기존 UI에 맞추되 공통 컴포넌트 옵션으로 전달합니다.
- hover, pressed, focus, disabled, loading은 10절의 공통 상태를 적용합니다.
- 비활성 버튼은 실제 동작도 차단합니다. 로딩 때 버튼 크기를 유지하고 `저장 중…`처럼 작업 상태를 표시합니다.
- 삭제 실행 전 확인 등 기존 기능 정책은 유지하며, 확인 UI를 사용하는 경우 공통 confirm을 사용합니다.
- 원본에서 지적한 주황 배경·흰색 작은 글씨의 대비는 통합 QA에서 확인합니다. 조정이 필요하면 공통 스타일에 일괄 반영합니다.

## 7. Inputs

| 속성 | 공통 기준 |
| --- | --- |
| 단일 행 높이 | 52px |
| 글씨 | 16px·400 |
| 배경 / 테두리 | Surface / Border, 권장 기본값 |
| radius / 좌우 패딩 | 10px / 16px, 권장 기본값 |
| 설명·오류 | 14px, 해당 입력창 아래 |

- 라벨을 입력창 위에 표시합니다. Placeholder로 라벨을 대체하지 않습니다.
- focus는 Primary 테두리와 눈에 띄는 포커스 표시를 함께 적용합니다.
- error는 Danger 테두리와 구체적인 오류 문구를 함께 표시합니다. 오류를 toast로만 알리지 않습니다.
- disabled는 사용 불가 상태를 표시하고 편집도 차단합니다.
- 선택 입력·검색 입력도 공통 폰트와 상태 표현을 공유합니다. 각 페이지에서 같은 유형을 새로 꾸미지 않습니다.
- 여러 줄 입력은 52px 고정 높이에서 제외합니다. 최소 높이 변형은 공통 컴포넌트에서 관리합니다.
- 비밀번호 표시/숨김 등 확정된 기능은 유지합니다. 검증 조건·값 보존 정책은 기능 명세를 따릅니다.

## 8. Cards와 Spacing

카드는 Surface 배경, **radius 20px**을 사용합니다. 테두리 `1px solid var(--color-border)`, 내부 패딩 24px, 모바일 16px은 원본 권장 기본값입니다. 그림자는 필요한 종류에만 `--shadow-common`을 적용합니다.

일러스트와 안내 문구를 담은 상태 카드(회원가입 완료, 회원가입 실패, 서버 오류 등)도 별도 컴포넌트가 아니라 같은 카드 변형입니다. 현재 이런 카드가 다른 카드보다 더 둥글고 그림자가 다르게 보이는 불일치가 발견되었으므로, radius 20px·패딩 24px·`--shadow-common`을 다른 카드와 동일하게 적용합니다.

| 관계 | 공통 기본 간격 | 참조 토큰 |
| --- | --- | --- |
| 아이콘과 글씨 | 12px | `--space-3` |
| 라벨과 입력창 | 12px | `--space-3` |
| 입력창과 보조 설명 | 12px | `--space-3` |
| 제목과 설명 | 12px | `--space-3` |
| 버튼 사이 | 12px | `--space-3` |
| 폼 항목 사이 | 16px | `--space-4` |
| 카드 패딩·카드 사이 | 24px | `--space-6` |
| 페이지 소개와 첫 콘텐츠 | 32px | `--space-8` |
| 큰 섹션 사이 | 32px | `--space-8` |

- 원본 간격 체계인 작은 간격 12~16px, 중간 간격 20~24px, 큰 간격 32px을 유지합니다.
- `--space-5: 20px`은 공통 변형에 필요할 때 사용합니다. 역할별 기본값을 페이지마다 20px/24px로 번갈아 쓰지 않습니다.
- 기존 이미지, 정보 순서, 버튼 위치를 유지하며 같은 종류의 카드는 같은 패딩·제목·그림자를 사용합니다.
- 클릭 기능이 없는 카드에는 클릭 가능한 것처럼 hover 효과나 손 모양 커서를 적용하지 않습니다.

## 9. Tables / Charts

원본의 색상·폰트·카드·간격을 재사용하는 공통 구현 규칙입니다. 테이블 컬럼, 차트 종류, 필터 조건과 데이터는 담당 페이지에서 정합니다.

### Tables

- 표 컨테이너는 공통 카드 스타일을 사용합니다. 표 제목은 섹션 또는 카드 제목의 역할에 맞춰 통일합니다.
- 헤더는 본문 16px·600, 셀은 본문 16px·400을 공통 기본값으로 사용합니다. 보조 정보는 14px을 사용합니다.
- 배경과 구분선은 Surface와 Border 토큰을 사용합니다. 셀 패딩은 공통 spacing에서 선택해 동일한 표 변형에 일괄 적용합니다.
- 텍스트는 왼쪽, 비교할 숫자는 오른쪽 정렬을 기본으로 하며 날짜·단위·소수점 형식은 같은 데이터끼리 통일합니다.
- 행 hover는 상호작용 가능한 행에만 공통 스타일로 표시합니다. 정렬 가능한 헤더는 현재 정렬 방향을 표시합니다.
- 데이터가 길면 표 영역 안에서 스크롤되게 합니다. 스크롤 때문에 페이지 전체가 옆으로 늘어나지 않게 합니다.
- 페이지네이션이 있는 화면은 공통 버튼·활성·disabled 스타일을 사용합니다. 원본 UI에 없는 페이지네이션을 추가하도록 요구하지 않습니다.
- 0건은 Empty, 조회 실패는 Error, 조회 중은 Loading으로 구분합니다.

### Charts

- 차트 제목·설명·범례·축 레이블의 폰트는 공통 Typography 역할을 사용합니다.
- 강조는 Primary, 보조 강조는 Secondary, 텍스트는 Text/Muted, 구분선은 Border를 사용합니다.
- 같은 지표와 범주는 페이지가 달라도 같은 색·단위·범례 순서를 사용합니다. 추가 시리즈 색상이 필요하면 공통 차트 팔레트로 등록합니다.
- Python 차트 설정에 CSS 변수를 직접 해석시킬 수 없는 경우 공통 어댑터에서 토큰 값을 변환·전달합니다. 페이지마다 HEX 값을 복제하지 않습니다.
- 카드 너비에 맞춰 차트를 조정하고 모바일에서 범례·축 레이블이 잘리지 않게 합니다.
- 필터·검색에 따른 선택 상태와 데이터 갱신 중 표시는 공통 컴포넌트를 사용합니다. 갱신 대상과 필터 결합 방식은 기능 명세를 따릅니다.
- 데이터가 없으면 빈 축이나 오해할 수 있는 0값 차트 대신 공통 Empty를 표시합니다.

## 10. 공통 상태 표현

상태는 기존 화면 안에서 표현하는 것을 기본으로 합니다. 이미 확정된 성공·실패 화면이 있으면 그 구성을 유지하고 같은 레이아웃과 컴포넌트를 적용합니다.

| 상태 | 공통 표현 | 구현 규칙 |
| --- | --- | --- |
| Default | 역할별 기본 스타일 | 페이지별 변형 금지 |
| Hover | 공통 강조 효과 | 실제 상호작용 가능한 요소에만 적용 |
| Pressed / Selected | 눌림 또는 선택 상태 표시 | 지속 선택은 색상 외 밑줄·아이콘·텍스트 등도 함께 사용 |
| Focus | 눈에 띄는 키보드 포커스 | 기본 표시를 제거한 채 대체 표시를 누락하지 않음 |
| Disabled | 비활성 외형 | 실제 클릭·입력 차단, hover/pressed보다 우선 |
| Loading | spinner와 작업 문구 | 중복 실행 차단, 가능하면 기존 영역 크기 유지 |
| Error | 오류 설명과 가능한 다음 행동 | 필드 오류는 입력 아래 유지, toast만으로 전달하지 않음 |
| Empty | 데이터 없음 안내 | 오류와 구분하고 기존 UI에서 가능한 조건 변경 등 안내 |
| Success | 완료 문구, 필요 시 공통 toast | 실제 완료 후 표시, 색상만으로 전달하지 않음 |

- 원본에 없는 hover·pressed·disabled 색상, success 전용 색상, focus 선 두께는 공통 담당자가 하나의 상태 스타일로 구현해 공유합니다. 페이지별 임의 값은 금지합니다.
- loading/disabled에서는 hover/pressed 효과를 억제하고, error 입력도 키보드 focus 위치는 식별 가능하게 합니다.
- loading은 요청 시작 시 표시하고 성공·실패 모두에서 종료합니다. 실패 후 재시도가 가능하도록 비활성 상태를 해제합니다.
- 선택한 평가·필터의 표시와 저장 완료 상태는 구분합니다. 처리되지 않은 작업을 완료처럼 표시하지 않습니다.
- toast의 위치·표시 방식은 `components.py`에서 관리합니다. 이동 후 메시지는 한 번만 표시되도록 공통 상태 처리를 사용합니다.
- 확인 UI가 필요한 기존 동작은 공통 confirm을 사용합니다. 결과 설명, 취소(Secondary), 실행(Danger)을 같은 형식으로 표시합니다.
- 연속 클릭·Streamlit 재실행이 작업을 중복 실행하지 않도록 UI 상태와 실제 요청 처리를 연결합니다. 조건·데이터 보존 정책 자체는 별도 명세에서 관리합니다.

## 11. Responsive

원본의 **768px 이하 좌우 16px, 페이지 제목 32px, 카드 패딩 16px** 권장값을 공통 기본 규칙으로 사용합니다.

| 화면 폭 | 적용 기준 |
| --- | --- |
| 데스크톱: 충분한 가용 너비 | 최대 1320px, 좌우 최소 44px, 페이지별 최대 너비 |
| 태블릿: 768px 초과의 좁은 화면 | 같은 너비 계산 유지, 기존 다단 배치가 넘치면 줄바꿈·세로 배치 |
| 모바일: 768px 이하 | 좌우 16px, 페이지 제목 32px, 카드 패딩 16px |

- 원본에는 별도 태블릿 breakpoint가 없습니다. 필요하면 공통 레이아웃 변형에 추가하고 전체 페이지에 공유합니다.
- 위 28px / 아래 35px, 단일 행 입력과 버튼 52px은 유지합니다.
- Navbar는 최소 높이 90px을 유지하되 메뉴 줄바꿈으로 높이가 늘어날 수 있습니다. 메뉴가 잘리거나 겹치지 않게 공통 처리합니다.
- 버튼 그룹·폼 컬럼·카드 그리드는 가용 너비에 맞춰 세로 배치합니다. 반응형에서도 기능과 필수 정보가 사라지지 않아야 합니다.
- 긴 문자열은 줄바꿈 또는 공통 말줄임 규칙을 사용합니다. 표의 가로 스크롤은 표 영역에 한정합니다.
- 모바일 조정과 breakpoint는 `styles.css`에서 관리합니다. 페이지별로 다른 모바일 토큰을 선언하지 않습니다.

## 12. Streamlit 공통 파일 구조

```text
common/
 ├─ styles.css
 ├─ navbar.py
 ├─ components.py
 └─ layout.py
```

| 파일 | 책임 | 페이지에서 전달하는 것 |
| --- | --- | --- |
| `styles.css` | 토큰, 폰트 로딩, 공통 외형·상태·반응형 | 공통 클래스/변형 선택 |
| `navbar.py` | 로고, 메뉴 구성, 활성 표시, 로그아웃 동작 연결 | 로그인 상태, 역할, 현재 메뉴, 동작 연결 |
| `components.py` | 버튼·입력·카드·toast·loading·empty·error·confirm 및 표/차트 공통 표현 | 라벨, 내용, 데이터, variant, 상태, 고유 key |
| `layout.py` | 공통 스타일 로딩, 바깥 너비, 페이지별 너비, 중앙 정렬과 여백 | 페이지 유형과 본문 구성 |

### 공통 적용 순서

1. 페이지 진입 시 `layout.py`를 통해 공통 스타일과 페이지 유형별 틀을 적용합니다.
2. `navbar.py`에 현재 화면 정보를 전달해 공통 헤더를 표시합니다.
3. 본문에서 `components.py`의 버튼·입력·카드·상태 표현을 사용합니다.
4. 페이지 담당자는 콘텐츠, 데이터, 기능 연결, 고유 배치만 구현합니다.

함수명과 인자는 공통 코드에서 확정해 팀에 공유합니다. 위 표는 인터페이스의 책임 기준이며 이미 존재하는 함수라고 가정한 호출 예제가 아닙니다.

### Streamlit 구현 시 필수 규칙

- 기본 위젯은 `.control-input`, `.control-button` 같은 CSS 클래스를 자동 적용하지 않습니다. 공통 담당자가 사용 중인 버전의 실제 위젯 구조에 맞춰 연결하고 대표 폼에서 검증합니다.
- 서로 다른 HTML 출력 호출에 여는 태그와 닫는 태그를 나누어 그 사이의 Streamlit 위젯을 감싸는 방식에 의존하지 않습니다. 실제 컨테이너에 공통 레이아웃을 적용합니다.
- 기본 여백과 공통 44px 여백이 중복되지 않도록 확인합니다.
- 스타일 로딩은 공통 진입 경로에서 관리합니다. 페이지마다 CSS를 재작성하거나 주입하지 않습니다. Streamlit 재실행 후에도 스타일이 적용되어야 합니다.
- 위젯 key는 페이지·컴포넌트별로 충돌하지 않게 지정합니다.
- 인증 정보, 현재 화면, 요청 상태, 결과, 일회성 메시지를 구분하고 화면 렌더링과 실제 요청 실행을 분리합니다.
- 비밀번호는 영구 저장·로그에 남기지 않습니다.
- Streamlit 버전과 CSS 연결 방식을 팀 전체가 공유합니다. 버전 변경 시 대표 5개 페이지와 상태 표현을 다시 확인합니다.
- 기존 공통 스타일 체계가 있으면 중복 체계를 만들지 않고 그곳에 이 기준을 반영합니다.

## 13. AI 구현 통일성을 위한 금지사항

AI(Codex/Claude 등)가 페이지를 구현하거나 수정할 때 다음을 금지합니다.

- 이 문서에 정의되지 않은 색상 HEX, 폰트, 그림자 값을 새로 만들어 사용하는 행위.
- 같은 역할의 버튼·입력창·카드에 페이지마다 다른 높이·radius·색상을 적용하는 행위.
- 같은 상태(성공/경고/오류/정보)에 서로 다른 색상을 사용하는 행위. 반드시 `--color-success`/`--color-warning`/`--color-danger`/`--color-info`만 사용합니다.
- 장식 목적의 그라데이션, 이모지, 애니메이션을 이 문서의 근거 없이 임의로 추가하는 행위.
- 같은 의미의 컴포넌트(버튼, 카드, 배지 등)를 새로운 형태로 중복 생성하는 행위.
- 차트마다 임의의 색상 팔레트를 새로 정의하는 행위. 9절의 공통 차트 팔레트만 사용합니다.
- 이 문서와 확정 UI 화면에 없는 기능, 화면, 버튼을 디자인 판단으로 추가하는 행위.
- 페이지별 필요·데이터·API·업무 규칙 판단은 이 문서의 범위가 아니며 별도 PRD를 따릅니다.

## 14. 팀원 구현 규칙

### 직접 스타일 지정 금지

- 페이지 코드에 색상 HEX/RGB, 폰트·글씨 크기, 버튼·입력 높이, radius·shadow를 반복 작성하지 않습니다.
- Navbar, 버튼, 입력창, 카드, toast를 페이지마다 별도 제작하지 않습니다.
- 페이지 CSS에서 공통 토큰이나 전역 Streamlit 위젯 스타일을 덮어쓰지 않습니다.
- 페이지별 임의 margin/padding 대신 관계에 맞는 공통 spacing을 사용합니다.
- HTML/차트 등 다른 렌더링 경로도 공통 스타일 또는 공통 어댑터를 사용합니다.

### 페이지 고유 구현 허용

- 기존 화면에 필요한 컬럼 수, 콘텐츠 배치, 정렬, 요소 순서.
- 페이지 고유 콘텐츠, 데이터, 기능과 이벤트 연결.
- 테이블 컬럼, 차트 종류, 필터 항목.
- 공통 기준을 지키는 페이지 전용 배치 클래스. 다른 페이지에 영향을 주지 않도록 범위를 한정합니다.

```css
/* 금지: 페이지에서 공통 카드 외형을 새 값으로 덮어쓰기 */
.main-result {
  background-color: #FF8A3D;
  border-radius: 12px;
}

/* 허용: 공통 카드를 재사용하고, 페이지에는 고유 배치만 정의 */
.main-result-grid {
  display: grid;
  gap: var(--space-6);
}
```

### 변경과 예외 처리

1. 공통 스타일 문제는 해당 공통 파일에서 수정합니다.
2. 필요한 변형이 없으면 공통 컴포넌트 옵션으로 추가하고 사용처를 공유합니다.
3. 기준 변경·예외는 적용 전에 팀에 공유하고 사유·화면·변경 값·담당자를 기록합니다.
4. 범위 안의 너비 변경도 공통 토큰에서만 수행합니다.
5. 공통 변경 후 영향을 받는 화면과 상태를 함께 검토합니다.

| 예외 기록 항목 | 작성 내용 |
| --- | --- |
| 대상 | 화면 및 컴포넌트 |
| 이유 | 공통 기본값으로 구현하기 어려운 구체적인 사유 |
| 변경 | 기존 기준, 변경값 또는 공통 변형 |
| 범위·담당 | 영향 화면과 유지보수 담당자 |
| 검토 | 데스크톱·모바일 및 관련 상태 확인 결과 |

## 15. 페이지별 적용 체크리스트

### 모든 페이지

- [ ] 확정 UI의 기능·정보 순서·고유 배치를 유지했다.
- [ ] 공통 layout → Navbar → components 적용 구조를 사용한다.
- [ ] Pretendard, 색상, 제목·본문·설명 스타일이 공통 기준과 같다.
- [ ] 페이지별 최대 너비와 바깥 정렬선이 맞고 패딩이 중복되지 않는다.
- [ ] 버튼·입력·카드·spacing에 페이지별 임의 스타일이 없다.
- [ ] 관련 hover/focus/disabled/loading/error/empty/success를 확인했다.
- [ ] 모바일 잘림·겹침·페이지 전체 가로 넘침이 없다.

### 로그인

- [ ] 로고 중심 단순 헤더와 로그인 기본 너비 1200px을 적용했다.
- [ ] 내부 폼은 확정된 배치를 유지하며 바깥 너비만큼 강제로 늘리지 않았다.
- [ ] 라벨·입력·오류 안내·제출 버튼이 공통 스타일이다.
- [ ] 로그인 실패 상태에서도 동일한 폼과 레이아웃 기준을 사용한다.

### 회원가입

- [ ] 단순 헤더와 회원가입 기본 너비 600px을 적용했다.
- [ ] 입력 항목 사이 16px, 라벨·설명 간격 12px을 적용했다.
- [ ] 확정된 성공·실패 화면에도 공통 제목·버튼·상태 표현을 적용했다.
- [ ] 검증 오류는 해당 필드 아래에서 확인 가능하다.

### 메인 맛집 추천

- [ ] 최대 너비 1320px, 맛집 추천 메뉴 활성 상태를 적용했다.
- [ ] 확정된 검색·필터·평가·추천 영역 구성을 유지했다.
- [ ] 추천 카드는 공통 제목·radius 20px·패딩을 사용한다.
- [ ] 선택 상태, 추천 중, 결과 없음, 조회 오류가 구분된다.
- [ ] 이 문서를 이유로 별도 버튼이나 상세 화면을 추가하지 않았다.

### 마이페이지·프로필 수정·비밀번호 변경

- [ ] 기본 너비 1000px과 마이페이지 메뉴 활성 상태를 공유한다.
- [ ] 정보 카드와 섹션의 제목·간격이 동일하다.
- [ ] 저장/변경은 Primary, 취소/보조 이동은 Secondary를 사용한다.
- [ ] 성공·실패 표현과 필드 오류가 공통 방식이다.
- [ ] 기존 위험 삭제 기능에 쓰이는 Danger·확인 UI가 공통 컴포넌트다.

### 관리자: 검색정보분석·식당정보·사용자피드백

- [ ] 최대 너비 1320px과 공통 Navbar 외형을 적용했다.
- [ ] 사이드바 폭·항목 높이·radius·활성 항목 표시가 검색정보분석·식당정보·사용자피드백 세 화면에서 동일하다.
- [ ] 만족/보통/불만족 등 상태를 나타내는 KPI 카드, 도넛차트, 배지가 `--color-success`/`--color-warning`/`--color-danger`만 사용한다.
- [ ] 확정된 관리자 하위 메뉴와 활성 표시를 일관되게 사용한다.
- [ ] 필터·검색·다운로드 버튼은 같은 공통 변형을 사용한다.
- [ ] 통계 카드, 표 제목·셀·구분선, 차트 폰트·색상·단위가 일관된다.
- [ ] 표 스크롤·페이지네이션이 있는 경우 공통 표현을 사용한다.
- [ ] 조회 중·빈 데이터·조회 오류 상태를 구분한다.

## 16. 통합 QA 체크리스트

검토 화면은 로그인·회원가입·메인·마이페이지·관리자와 확정된 하위/성공/실패 상태를 포함합니다.

### 시각적 일관성

- [ ] 화면을 나란히 비교했을 때 배경·폰트·Navbar 외형과 바깥 정렬선이 같다.
- [ ] 버튼·단일 행 입력은 52px, 컨트롤 radius는 10px, 카드 20px, Navbar 18px이다.
- [ ] 페이지 제목 40px, 섹션 24px, 카드 20px/강조형 22px, 본문 16px, 설명 14px이 맞다.
- [ ] 주요 여백은 역할별 12/16/24/32px을 적용했다.
- [ ] 같은 역할의 버튼·카드·표·차트에 서로 다른 스타일이 없다.
- [ ] 메뉴 활성 상태가 색상과 밑줄로 표시되고 하위 화면의 상위 메뉴도 맞다.

### 상태와 사용성

- [ ] 마우스 hover·pressed와 키보드 focus가 동일한 컴포넌트에서 일관된다.
- [ ] disabled·loading 중 실제 중복 동작이 차단되고 실패 후 다시 사용할 수 있다.
- [ ] 오류는 색상만으로 표시하지 않으며 중요한 오류가 toast로만 사라지지 않는다.
- [ ] 결과 없음과 조회 실패를 구분하고 기존 UI에서 가능한 다음 행동을 안내한다.
- [ ] 성공 표시는 실제 완료 이후에 나오며 일회성 메시지가 반복되지 않는다.
- [ ] 키보드 이동, 긴 텍스트, 주황 버튼의 글자 대비를 확인했다.

### 반응형과 공통 구조

- [ ] 넓은 데스크톱, 좁은 태블릿, 768px 경계와 모바일에서 확인했다.
- [ ] 모바일 좌우 16px·제목 32px·카드 패딩 16px 규칙을 확인했다.
- [ ] Navbar·버튼·폼이 겹치지 않고 표 가로 스크롤은 표 안에서만 발생한다.
- [ ] 페이지별 공통 스타일 복제나 전역 CSS 덮어쓰기가 없다.
- [ ] Streamlit 재실행 후에도 스타일·활성 메뉴·상태 표시가 유지된다.
- [ ] 연속 클릭·재실행이 저장·평가·삭제 등을 중복 실행하지 않는다.
- [ ] 공통 코드 또는 Streamlit 버전 변경 후 대표 5개 페이지를 확인했다.
- [ ] 필요한 예외를 기록했고 모든 팀원이 같은 공통 파일을 사용한다.

## 17. 적용 순서와 문서 검토 기록

1. 공통 담당자가 토큰·폰트·레이아웃·Navbar·기본 컴포넌트와 상태를 준비합니다.
2. 대표 페이지에서 실제 Streamlit 위젯의 크기·정렬·상태를 검증하고 팀 기준 화면으로 공유합니다.
3. 팀원이 담당 페이지에 공통 파일을 적용하고 15절의 페이지별 체크리스트를 확인합니다.
4. 통합 후 16절의 통합 QA 체크리스트로 비교하고 공통 요소의 차이는 공통 파일에서 수정합니다.

문서 재작성 검토:
- 원본의 색상·폰트·너비·높이·radius·shadow·spacing 토큰을 보존했습니다.
- 원본 권장값과 확정값을 구분하고, 표·차트 등 추가 구현 규칙을 명시했습니다.
- 화면 이동표·role 분기·세부 기능 처리 명세는 공통 UI 문서의 중심에서 제외했습니다.
- 공통 파일의 책임, 페이지 담당자의 허용 범위, 상태 표현, 페이지별·통합 체크리스트를 포함했습니다.
- 이 검토는 문서 내용 검토입니다. 실제 서비스의 화면 렌더링·동작 QA 완료를 의미하지 않습니다.
