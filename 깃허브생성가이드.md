# 깃허브 생성 가이드

> 📌 **팀당 저장소 1개.** 개인 계정이 아니라 **encore-ai-campus 조직 안에** 만듭니다.
> 저장소 이름은 `aio-01-p1-team{번호}` 고정입니다. → 예: `aio-01-p1-team3`
> 조직 github 레포지토리 규칙 숙지: github-organization-rules

---

## 1. 이름 · 설명 규칙

| 항목 | 규칙 | 예시 |
|---|---|---|
| **이름** | `{과정기수}-{회차}-{팀}` — 영문 소문자·하이픈만. **프로젝트명 넣지 않음** | `aio-01-p1-team1` |
| **Description** | 프로젝트가 무엇인지 한 줄. 미확정이면 임시명 후 나중에 수정 | `부동산 실거래가 분석 챗봇` |
| **공개 범위** | **Public** — 완성 후 포트폴리오로 쓸 수 있게 공개로 만듭니다 | — |
| **기본 브랜치** | `main` (생성 후 반드시 확인) | — |

---

## 2. 만드는 순서

**팀장 1명만** 아래를 진행합니다.

1. 조직 New repository 페이지 접속
2. **Owner를 `encore-ai-campus`로 선택** (개인 계정으로 만들면 규칙 위반 — 가장 흔한 실수)
3. Repository name = `aio-01-p1-team{번호}` / Description 입력 / **Public** 선택
4. **Add a README file** 체크 → Create repository
5. Settings에서 Default branch가 `main`인지 확인
6. **Settings → Collaborators and teams**에서 팀원을 추가하고 **Write** 권한 부여
7. 생성 완료를 강사·매니저에게 알림 → 권한 정리(팀장 Maintain 등)를 받습니다

> 🙋 2번에서 `encore-ai-campus`가 보이지 않거나 3번에서 **Public이 선택 불가**하면 조직 권한 설정 때문입니다. **직접 만들지 말고** 팀 번호·이름·Description을 정리해 강사/매니저에게 생성 요청하세요. 학생에게는 Owner·Admin 권한이 부여되지 않습니다.

---

## 3. 팀원이 할 일

```bash
# 초대 수락 후
git clone https://github.com/encore-ai-campus/aio-01-p1-team1.git
cd aio-01-p1-team1
git switch -c feature/collect-api   # 작업 브랜치 생성
# ... 작업 ...
git add . && git commit -m "feat: 공공데이터 API 수집 스크립트 추가"
git push -u origin feature/collect-api
# GitHub에서 PR 생성 → 팀원 1명 이상 리뷰 → main에 merge
```

---

## 4. 첫 커밋 전 필수 세팅

### `.gitignore`

> ⚠️ **저장소가 Public이므로 올라간 키는 전 세계에 공개됩니다.** 실수로 커밋했다면 삭제로 끝내지 말고 즉시 키를 폐기·재발급하세요.

```
.env
.venv/
__pycache__/
chroma_db/
data/raw/
.ipynb_checkpoints/
.DS_Store
```

### 권장 폴더 구조

```
├── README.md
├── .env.example        # 키 없이 변수 이름만
├── pyproject.toml
├── data/               # raw는 커밋 제외, 명세서는 포함
├── notebooks/          # 데이터 분석용
├── src/                # collect / preprocess / index / rag / eval
└── app.py              # Streamlit 통합 앱
```

---

## 5. 제출 전 체크리스트

- [ ] 저장소가 `encore-ai-campus` 조직 아래에 있다
- [ ] 이름이 `aio-01-p1-team{번호}` 형식이다
- [ ] Description이 채워져 있다
- [ ] 팀원 전원이 접근 가능하다
- [ ] `main`에 직접 push한 이력이 없고, PR·리뷰 이력이 남아 있다
- [ ] `.env`와 API 키가 저장소에 없다 (커밋 이력 포함)
- [ ] README.md가 작성되어 있다
