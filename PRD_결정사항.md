# PRD 결정사항 기록

이 문서는 PRD 전체 개정 과정에서 기능·API·DB·화면·운영 방식에 영향을 주는 결정을 기록한다. 단순한 문장 정리나 표현 변경은 기록하지 않는다.

## 상태 정의

| 상태 | 의미 |
| --- | --- |
| 확정 | 사용자 제공 기준이나 공식 평가기준으로 결정된 사항 |
| 적용 | 명시된 기준을 충족하기 위해 PRD 개정본에 적용한 사항 |
| 확인 필요 | 선택지에 따라 기능·DB·일정이 달라져 사용자 또는 팀의 결정이 필요한 사항 |
| 보류 | 정보가 부족하여 구현 범위에 넣지 않는 사항 |
| 제외 | 이번 프로젝트에서 구현하지 않기로 정해진 사항 |
| 변경·일부 변경 | 뒤의 결정으로 기존 결정의 전부 또는 일부가 바뀐 사항 |
| 추후 결정 | 현재 구현을 막지 않아 이후 입력하기로 한 운영 정보 |

## 현재 결정사항

| ID | 상태 | 결정사항 | 근거·영향 |
| --- | --- | --- | --- |
| `DEC-001` | 확정 | P1-1~P1-4와 M0~M9를 최상위 기준으로 사용한다. | 기존 PRD와 충돌하면 PRD를 수정한다. |
| `DEC-002` | 확정 | `encore_project.xlsx`의 `정의서_new` 탭을 DB 핵심 참조자료로 사용한다. | 다른 DB 자료와 충돌하면 `정의서_new`를 우선 검토한다. |
| `DEC-003` | 확정 | `design.md`를 완성된 공통 UI 구현 기준으로 사용한다. | PRD는 기능·데이터·API·업무 규칙을 담당하고 디자인 값을 중복 정의하지 않는다. |
| `DEC-004` | 확정 | 함수명은 영문 소문자 `snake_case`와 `동작_대상` 형식을 사용한다. | CRUD와 공통 동사 접두어를 팀 컨벤션에 맞춘다. |
| `DEC-005` | 확정 | 저장소는 `encore-ai-campus` 조직의 `aio-01-p1-team{번호}` 형식으로 운영한다. | Public, `main`, 기능 브랜치·PR·리뷰 규칙을 적용한다. |
| `DEC-006` | 적용 | 프로젝트를 사용자용 맛집 추천과 운영용 로그 분석의 두 축으로 정의한다. | 기존 추천 기능을 유지하면서 공식 로그 대시보드 과제를 프로젝트 중심 범위에 포함한다. |
| `DEC-007` | 적용 | 프로젝트명을 `PlayEAT: 신대방 점심 맛집 추천 및 AI 서비스 로그 분석 대시보드`로 개정한다. | `design.md`의 PlayEAT 명칭과 공식 로그 대시보드 범위를 함께 표현한다. |
| `DEC-008` | 적용 | 핵심 사용자·관리자 데이터 요청은 FastAPI를 통과한다. | Streamlit의 DB 직접 조회로 로그 수집이 우회되는 상황을 방지한다. |
| `DEC-009` | 적용 | 사용자 추천 만족도와 LLM 로그 요약 품질평가를 별도 지표로 관리한다. | M6은 정답이 있는 로그·요약 사례를 이용한 사실 일치도 측정을 요구한다. |
| `DEC-010` | 일부 변경 | API 요청 로그, 추천 기록, 사용자 피드백, LLM 평가 기록을 서로 다른 데이터 개념으로 관리한다. | 거절 이유 기록은 이후 사용자 결정으로 제거됐다. |
| `DEC-011` | 적용 | 관리자는 `profiles.profile_type = '0'`, 일반 사용자는 `'1'`로 판정한다. | `정의서_new`의 코드 체계를 따른다. 백엔드에서도 권한을 검증한다. |
| `DEC-012` | 확정 | 비밀번호는 Supabase Auth가 관리하며 서비스 DB와 로그에 저장하지 않는다. | `정의서_new`의 시스템 참조 기준과 인증정보 보호 원칙을 따른다. |
| `DEC-013` | 적용 | 로그에 비밀번호·Access Token·OAuth 인증 코드·API Key를 저장하지 않는다. | FastAPI 요청·응답 로깅 과정의 인증정보 노출을 방지한다. |
| `DEC-014` | 제외 | 방문자 수·피크 시간대·시간대별 통계와 `Dashboard_Daily_Stat`을 구현하지 않는다. | `정의서_new`의 폐기 기록을 따른다. |
| `DEC-015` | 보류 | `Dashboard_personal`은 요구사항과 컬럼이 확정되기 전까지 추가하지 않는다. | `정의서_new`에 테이블명만 있고 정의가 없다. |

## 개정 중 주요 결정 상태

| ID | 상태 | 결정 필요사항 | 선택에 따른 영향 |
| --- | --- | --- | --- |
| `DEC-P01` | 적용 | 추천 결과를 영구 저장할 `recommendations` 테이블을 신규 추가한다. | 같은 대화의 이전 추천 제외와 특정 추천 결과·피드백 연결에 필요하다. 정확한 스키마는 DB 개정에서 확정한다. |
| `DEC-P02` | 적용 | 상황·메뉴 특성은 별도 `restaurant_situations` 테이블을 만들지 않고 `tag_categories`와 `restaurant_tags`에 통합한다. | 기존 DB 구조를 재사용하고 태그 유형으로 가격·맛·상황·목적을 구분한다. |
| `DEC-P03` | 적용 | 영업시간·브레이크타임·출처·확인일은 식당에, 메뉴 출처·확인일은 메뉴에 저장한다. | MVP에서 값의 소유 단위와 최신성 판정을 명확하게 유지한다. |
| `DEC-P04` | 적용 | MVP 일반 로그인은 이메일과 비밀번호를 사용하고 별도 로그인 아이디는 입력받지 않는다. | 기존 로그인 흐름과 Supabase Auth 구조가 이메일 인증을 사용하며 `profile_login_id`는 선택 컬럼이다. |
| `DEC-P05` | 적용 | API 로그에 요청·응답 본문을 저장하지 않는다. | `DEC-032`로 상세 계약을 확정했다. |
| `DEC-P06` | 적용 | 원본 로그 30일 보존, 관리자 수동 정제, 정제 실행 기반 통계를 기본으로 한다. | 사용자가 30일 보존으로 확정했다. |
| `DEC-P07` | 적용 | 최소 10개 사례, 사실 60%·완전성 30%·근거 10%, 총점·사실성 80점 이상을 합격으로 한다. | M6·M7을 동일 기준으로 재현한다. |
| `DEC-P08` | 적용 | Redis 장애 시 PostgreSQL 폴백, TTL은 프로필 15분·대화/추천 24시간·멱등성 10분이다. | `DEC-030`·`DEC-031`로 확정했다. |

## 개정 단위 003에서 추가된 결정

| ID | 상태 | 결정사항 | 근거·영향 |
| --- | --- | --- | --- |
| `DEC-016` | 적용 | 추천 화면은 명시적 선택 조건을 자연어 추출 조건보다 우선한다. | 사용자가 직접 선택한 값을 Gemini가 임의로 덮어쓰지 않게 한다. |
| `DEC-017` | 적용 | 추천 후보가 없을 때 조건을 자동 완화하지 않는다. | 사용자가 알지 못하는 조건 변경과 설명 불일치를 방지한다. |
| `DEC-018` | 변경 | 같은 대화의 이전 추천을 제외하되 후보가 모두 소진되면 가장 오래된 추천부터 자동 재포함한다. | 사용자 결정 46-B가 수동 초기화 방식을 대체했다. |
| `DEC-019` | 적용 | 추천 후보 조회와 필터링은 FastAPI의 결정 가능한 로직이 수행한다. | Gemini는 조건 추출과 설명 생성에 한정하여 DB 밖의 식당 정보 생성을 방지한다. |
| `DEC-P09` | 적용 | 약관 종류·버전·동의 여부·동의 시각을 `user_consents` 이력으로 저장한다. | 필수 동의의 현재 버전을 검증하고 변경 이력을 보존한다. |

## 개정 단위 004에서 추가된 결정

| ID | 상태 | 결정사항 | 근거·영향 |
| --- | --- | --- | --- |
| `DEC-020` | 적용 | Kakao Local API에서 사진 경로를 수집하지 않는다. | 공식 장소 검색 응답에 사진 필드가 없다. 이미지는 직접 확보한 경우에만 저장한다. |
| `DEC-021` | 적용 | `restaurants.storage_path`는 선택값으로 변경하고 값이 없으면 화면에서 공통 기본 이미지를 사용한다. | API에서 이미지를 제공하지 않으므로 현재 필수 제약은 실제 데이터 수집과 충돌한다. DB 정의서 변경이 필요하다. |
| `DEC-022` | 적용 | 데이터 품질의 완성 식당은 카테고리·유효 가격 메뉴 1개 이상·태그 1개 이상을 모두 가진 식당으로 계산한다. | 보류된 `complete_count` 산정 기준을 재현 가능하게 만든다. |
| `DEC-023` | 제외 | 추천 거절 이유 입력·저장·통계를 구현하지 않는다. | 사용자가 팀에서 거절 이유를 삭제했다고 확정했다. |
| `DEC-024` | 적용 | `feedbacks` 표기를 `feedback`으로 통일하고 특정 추천을 가리키는 `recommendation_id`를 추가한다. | `정의서_new`의 테이블명을 따르고 동일 대화·식당의 여러 추천을 구분한다. |
| `DEC-025` | 적용 | 동일 사용자의 동일 추천 평가는 새 행을 추가하지 않고 기존 값을 수정한다. | `updated_at`의 목적을 명확히 하고 중복 평가 집계를 방지한다. |
| `DEC-P10` | 적용 | MVP에서는 출처·확인일을 식당과 메뉴에 직접 저장한다. | 별도 출처 이력 테이블의 복잡도를 피하고 컬럼 단위 출처를 유지한다. |
| `DEC-P11` | 적용 | 포함 태그는 최소 1개 일치를 후보 조건으로 하고 일치 개수로 우선순위를 정한다. | 후보가 과도하게 사라지는 문제를 줄이면서 결과를 재현한다. |
| `DEC-P12` | 제외 | 거리 계산·거리 필터·거리 기반 거절 사유를 프로젝트 범위에서 제거한다. | 사용자가 거리 계산을 제외하기로 확정했다. |

## 개정 단위 005에서 추가된 결정

| ID | 상태 | 결정사항 | 근거·영향 |
| --- | --- | --- | --- |
| `DEC-026` | 적용 | 마이페이지의 개인 통계는 방문 기록이 아니라 추천 조건과 긍정 평가를 기반으로 계산한다. | 서비스에 실제 방문 확인 데이터가 없으므로 방문·선택으로 과장하지 않는다. |
| `DEC-027` | 적용 | 자주 사용한 태그는 저장된 최종 추천 조건의 태그 사용 횟수 상위 5개로 계산한다. | 재현 가능한 원본 데이터와 계산 기준을 제공한다. |
| `DEC-028` | 적용 | 선호 음식 카테고리 비율은 긍정 평가 식당의 카테고리 분포로 계산한다. | 단순 추천 노출보다 사용자의 긍정 반응을 근거로 한다. |
| `DEC-029` | 적용 | 최근 대화와 추천 제외 캐시는 사용자 단위가 아니라 대화 단위로 분리한다. | 서로 다른 대화의 문맥·추천 이력이 섞이는 문제를 방지한다. |
| `DEC-030` | 적용 | Redis 장애 시 PostgreSQL을 기준으로 복구하고 Redis 저장 실패가 영구 DB 저장 성공을 취소하지 않게 한다. | Redis를 가속용 임시 저장소로 유지한다. |
| `DEC-031` | 적용 | 캐시 기본 TTL은 프로필 15분, 대화·추천 제외 24시간, 중복 방지 10분으로 둔다. | 개발·시연 환경에서 갱신성과 복구 가능성을 함께 확보한다. 값은 환경설정으로 관리한다. |
| `DEC-P13` | 적용 | 비밀번호 변경 성공 후 모든 세션을 종료한다. | 기존 세션이 계속 유효한 상황을 줄인다. |
| `DEC-P14` | 제외 | 회원 탈퇴 기능을 구현하지 않는다. | 사용자가 탈퇴 기능 전체를 제거했다. |
| `DEC-P15` | 제외 | 탈퇴 데이터 보존·삭제 정책은 적용하지 않는다. | 회원 탈퇴 기능 제거에 따라 폐기됐다. |

## 개정 단위 006에서 추가된 결정

| ID | 상태 | 결정사항 | 근거·영향 |
| --- | --- | --- | --- |
| `DEC-032` | 적용 | API 로그에는 요청·응답 본문을 기본 저장하지 않고 메타데이터와 표준 에러 코드만 저장한다. | M0~M5에 필요한 운영 분석을 지원하면서 비밀번호·토큰·개인정보 노출을 줄인다. |
| `DEC-033` | 적용 | 원본 API 로그를 수정하지 않고 정제 실행과 로그별 정제 결과를 별도 테이블로 저장한다. | M2의 제거·정규화 결과와 M3 통계를 재현할 수 있다. |
| `DEC-034` | 적용 | 제품 검색 통계와 API 운영 통계를 별도 테이블로 관리한다. | `dashboard_search_stats`의 음식·가격·상황 통계를 응답시간·에러율과 혼합하지 않는다. |
| `DEC-035` | 적용 | LLM 요약과 원본 로그는 다대다 근거 연결 테이블로 관리한다. | 요약의 핵심 주장별 근거를 화면과 평가에서 추적한다. |
| `DEC-036` | 적용 | 개선 실험은 개선 전·후 버전과 동일 평가 사례의 실행 결과를 연결해 저장한다. | M7의 전후 비교를 데이터로 재현한다. |
| `DEC-037` | 적용 | `dashboard_search_stats`에 `(stat_date, stat_type, stat_key)` UNIQUE를 적용한다. | 동일 집계의 중복 저장을 방지한다. |
| `DEC-038` | 적용 | 식당 원본 주소와 서비스 표시 주소를 구분하고 Kakao 장소 ID·좌표·URL을 저장한다. | API 원본 추적과 Kakao 장소 링크에 사용한다. 좌표는 수집 원본으로만 보존하고 거리 계산에는 사용하지 않는다. |
| `DEC-P16` | 적용 | 식당 삭제는 물리 삭제 대신 `is_active=false` 비활성화로 처리한다. | 과거 추천·평가 재현성을 유지한다. |
| `DEC-P17` | 적용 | MVP 정제는 관리자 수동 실행, 통계는 성공한 정제 실행 ID 기준으로 계산·저장한다. | 시연 순서와 결과 재현성을 명확히 한다. |
| `DEC-P18` | 적용 | 사실 일치도 60%·완전성 30%·근거 연결 10%로 총점을 계산한다. | M6·M7의 비교 기준을 수치로 고정한다. |

## 개정 단위 007에서 추가된 결정

| ID | 상태 | 결정사항 | 근거·영향 |
| --- | --- | --- | --- |
| `DEC-039` | 적용 | API 기본 prefix를 `/api/v1`로 사용한다. | 버전 변경 시 기존 클라이언트와 계약을 분리한다. |
| `DEC-040` | 적용 | 대화 목록은 `GET /conversations`, 대화 상세은 `GET /conversations/{conversation_id}`로 바로잡는다. | 기존 보조 API 명세의 기능 설명이 경로와 뒤바뀌어 있다. |
| `DEC-041` | 적용 | 추천 평가는 `/recommendations/{recommendation_id}/feedback`의 단일 하위 리소스로 관리한다. | 추천 한 건당 사용자 평가 한 건이라는 DB UNIQUE 규칙과 일치한다. |
| `DEC-042` | 적용 | API 성공·오류 응답에 `request_id`와 `timestamp`를 포함한다. | 화면 오류와 원본 API 로그를 연결해 추적한다. |
| `DEC-043` | 적용 | 생성·실행 API는 `X-Idempotency-Key`를 지원한다. | Streamlit 재실행·연속 클릭의 중복 추천·평가·실행을 방지한다. |
| `DEC-044` | 적용 | 시간이 걸리는 정제·요약·평가·실험은 실행 리소스를 생성하고 상태를 조회한다. | 요청 timeout과 중복 실행을 줄이고 진행 상태를 화면에 표시한다. |
| `DEC-045` | 적용 | 외부 서비스 오류를 502·503·504로 구분하고 표준 내부 에러 코드로 변환한다. | 모든 외부 실패를 500으로 처리하지 않고 사용자 행동과 운영 분석을 일관되게 만든다. |
| `DEC-046` | 적용 | 추천 후보 없음은 성공 응답의 구조화된 Empty 데이터로 처리한다. | 유효한 조건에서 후보가 없는 것은 시스템 장애나 존재하지 않는 요청 리소스가 아니며 API 에러율을 왜곡하지 않는다. |
| `DEC-047` | 적용 | 사용자가 소유하지 않은 대화·추천·평가 조회는 리소스 존재를 숨기는 `404 RESOURCE_NOT_FOUND`로 통일한다. | 타인 리소스 존재 여부 노출을 줄이고 소유권 검사를 일관되게 적용한다. |
| `DEC-P21` | 적용 | callback은 실제 토큰 대신 60초 단일 사용 handoff code를 전달하고 Streamlit이 서버에서 세션으로 교환한다. | URL과 브라우저 기록에 Access Token이 노출되는 것을 막는다. |

## 개정 단위 008에서 추가된 결정

| ID | 상태 | 결정사항 | 근거·영향 |
| --- | --- | --- | --- |
| `DEC-048` | 적용 | 평가 필수 M0~M9와 P1-1~P1-4 전체를 MVP P0로 둔다. | 부가 인증·관리 기능 때문에 제출 필수 흐름이 미완성되는 것을 막는다. |
| `DEC-049` | 적용 | 관리자 대시보드의 API 운영 지표와 추천 제품 지표를 별도 화면 영역 또는 탭으로 구분한다. | 응답시간·에러율과 만족도·거절률의 의미 및 분모 혼합을 막는다. |
| `DEC-050` | 적용 | 대시보드 최초 조회 기간은 최근 7일, 원본 로그 기본 정렬은 최신순으로 한다. | 구현 도구별 초기 화면 차이를 제거하고 즉시 확인 가능한 기본값을 제공한다. |
| `DEC-051` | 적용 | 자동 새로고침은 MVP 기본 비활성이고 수동 새로고침 완료 시 마지막 갱신 시각을 표시한다. | Streamlit 재실행 부하와 화면 흔들림을 줄이고 시연 상태를 명확히 한다. |
| `DEC-052` | 적용 | 드래그는 MVP 필수 업무 동작으로 사용하지 않는다. 차트 라이브러리에서 제공할 경우 범위 선택과 초기화만 보조 기능으로 허용한다. | P1-2의 액션 반응을 명시하면서 Streamlit에서 불안정한 커스텀 구현을 피한다. |
| `DEC-053` | 적용 | P0 인증 범위는 이메일 회원가입·로그인이며 Kakao OAuth는 P1로 조정한다. | 배포 로그 분석과 추천 핵심 흐름을 먼저 완성하고 외부 callback 위험을 분리한다. |
| `DEC-054` | 적용 | 구현 에이전트는 PRD·design·API·DB 정의서·컨벤션·Git 가이드를 함께 읽고 문서 우선순위를 따른다. | Codex와 Claude가 페이지별로 서로 다른 계약을 추정하지 않게 한다. |
| `DEC-P22` | 일부 변경 | API 원본 로그는 발생일부터 30일 보존한다. | 회원 탈퇴 관련 UUID 처리 규칙은 탈퇴 기능 제거로 폐기됐다. |
| `DEC-P23` | 부분 적용 | M6 사례 수·합격 점수는 확정했으며 M0~M9 담당자는 팀 입력값으로 남긴다. | 평가 계약은 구현 가능하고 사람 이름은 팀 배정 후 입력해야 한다. |

## 개정 단위 009에서 추가된 결정

| ID | 상태 | 결정사항 | 근거·영향 |
| --- | --- | --- | --- |
| `DEC-055` | 적용 | 직접 조사 데이터 최신성 기본 기준은 30일이다. | 오래된 메뉴·가격을 최신 정보처럼 사용하지 않는다. |
| `DEC-056` | 적용 | 추천 정렬은 필수 조건→포함 태그 일치 수→확인일→식당 ID 순서이며 무작위 선택을 쓰지 않는다. | 같은 입력에서 같은 결과를 재현하고 테스트한다. |
| `DEC-057` | 적용 | 장기 작업은 실행 레코드를 먼저 저장한 뒤 MVP에서는 FastAPI 배경 작업으로 처리한다. | 별도 큐 도입 전에도 진행·실패 상태와 재실행을 제공한다. |
| `DEC-058` | 적용 | M7 성공은 같은 평가 기준에서 평균 총점 5점 이상 상승하고 사실 일치도가 하락하지 않는 경우다. | 작은 변동이나 사실성 손상을 개선으로 오판하지 않는다. |
| `DEC-059` | 일부 변경 | 일반 대화 삭제와 식당 삭제는 soft delete·비활성화한다. 회원 탈퇴 관련 정책은 적용하지 않는다. | 회원 탈퇴 기능이 이후 제거됐다. |
| `DEC-060` | 적용 | 회원가입 약관 동의는 `user_consents`에 종류·버전·동의 여부·시각을 저장한다. | 필수 동의 여부와 문서 버전을 재현한다. |
| `DEC-061` | 적용 | Kakao OAuth callback은 60초 단일 사용 handoff code 방식으로 Streamlit 세션에 연결한다. | 실제 인증 토큰의 URL 노출을 막는다. |

## 개정 단위 010에서 추가된 결정

| ID | 상태 | 결정사항 | 근거·영향 |
| --- | --- | --- | --- |
| `DEC-062` | 확정 | API 원본 로그 보존기간은 30일이다. | 사용자 결정 5를 반영했다. |
| `DEC-063` | 확정 | 직접 조사 데이터 최신성 기준은 30일이다. | 사용자 결정 6을 반영했다. |
| `DEC-064` | 확정 | 최신성 30일 초과 데이터도 경고 없이 추천에 사용한다. 출처 없음·형식 오류 데이터는 추천에서 제외한다. | 사용자가 경고가 필요 없다고 확정했다. |
| `DEC-065` | 확정 | 거리 계산·거리 필터·`TOO_FAR` 거절 사유를 전부 제외한다. | 사용자 결정 8~10을 반영했다. |
| `DEC-066` | 확정 | M6은 최소 10개 사례와 사실 60%·완전성 30%·근거 10%를 사용한다. | 사용자 결정 11~12가 권장안으로 승인됐다. |
| `DEC-067` | 확정 | M6 합격은 총점 80점 이상이면서 사실 일치도 80점 이상이다. | 사용자 결정 13이 권장안으로 승인됐다. |
| `DEC-068` | 확정 | M7 성공은 평균 총점 5점 이상 상승하고 사실 일치도가 하락하지 않는 경우다. | 사용자 결정 14가 권장안으로 승인됐다. |
| `DEC-069` | 확정 | MVP 장기 작업은 실행 레코드 생성 후 FastAPI 배경 작업으로 처리한다. | 사용자 결정 15가 권장안으로 승인됐다. |

## 개정 단위 011에서 추가된 결정

| ID | 상태 | 결정사항 | 근거·영향 |
| --- | --- | --- | --- |
| `DEC-070` | 일부 변경 | 가격대 금액 경계는 저가 12,000원 이하, 중가 12,001~20,000원, 고가 20,001원 이상이다. API 값은 문자열 코드 대신 DB 가격대 태그 ID를 사용한다. | 사용자의 금액 경계와 기존 DB 판정 구조를 함께 반영했다. |
| `DEC-071` | 확정 | 가격대는 필수 단일 선택이며 `가격 무관`을 제공하지 않는다. | 사용자 결정 22-A를 반영했다. |
| `DEC-072` | 확정 | 음식 카테고리는 필수 단일 선택이며 `전체 음식`을 제공하지 않는다. | 사용자 결정 23-A를 반영했다. |
| `DEC-073` | 확정 | 포함 태그를 선택하지 않으면 카테고리와 가격만으로 추천한다. | 사용자 결정 25-A를 반영했다. |
| `DEC-074` | 확정 | 최신성 30일 초과 데이터에 사용자·관리자 경고를 표시하지 않는다. | 사용자 결정 27을 반영했다. |

## 개정 단위 012에서 추가된 결정

| ID | 상태 | 결정사항 | 근거·영향 |
| --- | --- | --- | --- |
| `DEC-075` | 제외 | 회원 탈퇴 기능 제거로 인증 계정 삭제 정책을 폐기한다. | 최신 사용자 결정을 반영했다. |
| `DEC-076` | 제외 | 회원 탈퇴 기능 제거로 대화·메시지 탈퇴 삭제 정책을 폐기한다. | 최신 사용자 결정을 반영했다. |
| `DEC-077` | 제외 | 회원 탈퇴 기능 제거로 추천·평가 삭제 정책을 폐기한다. | 최신 사용자 결정을 반영했다. |
| `DEC-078` | 제외 | 회원 탈퇴 기능 제거로 탈퇴 사용자 로그 정책을 폐기한다. | 최신 사용자 결정을 반영했다. |
| `DEC-079` | 확정 | 비밀번호는 조합 조건 없이 8자 이상이다. | 사용자 결정 20-A를 반영했다. |
| `DEC-080` | 확정 | 태그 일치 수가 가장 많은 식당 1곳을 추천하고 동점은 확인일·식당 ID로 결정한다. | 사용자 결정 24-A·26을 반영했다. |
| `DEC-081` | 확정 | 시연용 약관 문구와 `terms-v1.0`·`privacy-v1.0` 버전을 프로젝트에 포함한다. | 사용자 결정 28-A를 반영했다. 시행일은 배포일이다. |
| `DEC-082` | 확정 | Kakao OAuth는 P1으로 유지한다. | 사용자 결정 29-A를 반영했다. |
| `DEC-083` | 확정 | 마이페이지 프로필과 개인 통계 전체를 P0로 구현한다. | 사용자 결정 30-B를 반영했다. |
| `DEC-084` | 확정 | 관리자 식당 조회는 P0, 생성·수정·비활성화는 P1이다. | 사용자 결정 31-A를 반영했다. |
| `DEC-085` | 확정 | Streamlit Community Cloud·Render·Supabase·`main` 배포 구성을 사용하고 Redis 서비스만 추후 결정한다. | 사용자 결정 32를 반영했다. |
| `DEC-086` | 확정 | GitHub 팀 번호는 1번이며 저장소명은 `aio-01-p1-team1`이다. | 사용자 결정 33을 반영했다. Git 가이드에 따라 Public·main·PR 규칙을 적용한다. |
| `DEC-087` | 제외 | DB 파일 포함 방식을 추가 선택사항으로 관리하지 않는다. | 사용자 결정 34를 반영했다. 핵심 DB 참조자료의 효력은 유지한다. |
| `DEC-P24` | 추후 결정 | M0~M9 팀 담당자와 발표 분담은 나중에 정한다. | 사용자 결정 35를 반영했다. |

## 개정 단위 013에서 추가된 결정

| ID | 상태 | 결정사항 | 근거·영향 |
| --- | --- | --- | --- |
| `DEC-088` | 확정 | 회원 탈퇴 화면·API·DB 처리 정책을 프로젝트에서 전부 제외한다. | 사용자가 탈퇴 기능 삭제를 확정했다. |
| `DEC-089` | 확정 | 가격대는 메뉴 가격 재계산보다 DB의 기존 상·중·하 태그 판정값을 우선하며 API는 태그 ID를 전달한다. | 사용자 결정 38과 `정의서_new` 관계 구조를 반영했다. |
| `DEC-090` | 제외 | 가격 미판정 식당에 별도 사용자 처리 기능을 만들지 않는다. | 사용자 결정 39를 반영했다. 데이터 품질 검증에서만 판정 누락을 확인한다. |
| `DEC-091` | 확정 | 음식 카테고리는 한식·일식·중식·양식·기타로 확정한다. | 사용자 결정 40을 반영했다. |
| `DEC-092` | 확정 | 거절 이유 입력·저장·통계와 `recommendation_rejections` 테이블을 제거한다. | 사용자 결정 41을 반영했다. |
| `DEC-093` | 확정 | Gemini 추천 이유 생성 실패 시 DB 사실만 사용한 고정 형식 설명을 제공한다. | 사용자 결정 42-B를 반영했다. |
| `DEC-094` | 확정 | 추천 이유는 1~3문장, 최대 300자다. | 사용자 결정 43-A를 반영했다. |
| `DEC-095` | 확정 | 자연어 추가 입력은 선택이다. | 사용자 결정 44-A를 반영했다. |
| `DEC-096` | 확정 | 포함 태그는 최대 3개다. | 사용자 결정 45-A를 반영했다. |
| `DEC-097` | 확정 | 이전 추천으로 후보가 소진되면 가장 오래된 추천부터 자동 재포함한다. | 사용자 결정 46-B를 반영했다. |
| `DEC-098` | 확정 | 약관 운영자 표시는 `PlayEAT 프로젝트팀`으로 한다. | 사용자 결정 47-A를 반영했다. |

## 개정 단위 014에서 추가된 결정

| ID | 상태 | 결정사항 | 근거·영향 |
| --- | --- | --- | --- |
| `DEC-099` | 확정 | 원본 `분식`·`카페`는 보존하고 서비스 표시·필터·추천에서는 `기타`로 통합한다. | 사용자 결정 48-A를 반영했다. |
| `DEC-100` | 확정 | 현재 PRD를 `PRD-BASELINE-1.0` 구현 기준선으로 확정하고 추가 기능 결정을 중단한다. | 사용자가 현재 상태 확정을 요청했다. |
| `DEC-101` | 확정 | 기준선 이후에는 평가기준 위반·구현 불가능·치명적 보안 문제·명백한 문서 충돌만 수정한다. | 구현 중 범위 확장을 방지한다. |

## 결정 기록 규칙

- 새 결정이 발생하면 `DEC-###` 또는 `DEC-P##`로 기록한다.
- 사용자·팀이 선택해야 하는 사항은 PRD에 확정값처럼 쓰지 않고 `확인 필요`로 유지한다.
- 결정이 확정되면 관련 PRD 요구사항, DB 정의서, API 명세, 화면 명세를 함께 갱신한다.
- 기존 결정을 변경하면 이전 내용을 삭제하지 않고 상태와 변경 이유를 기록한다.
