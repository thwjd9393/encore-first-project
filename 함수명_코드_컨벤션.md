# 함수명 코드 컨벤션

## 1\. 기본 규칙

함수명은 \*\*영어 소문자 \+ `_`\*\*를 사용합니다.

✅ 좋은 예

get\_user()

create\_restaurant()

send\_message()

❌ 나쁜 예

getUser()

GetUser()

getuser()

가장 중요한 규칙은 **함수 이름만 보고 무슨 일을 하는지 알 수 있게** 작성하는 것입니다.

---

## 2\. CRUD 함수명은 통일하기

DB 데이터를 생성·조회·수정·삭제할 때는 아래 이름을 사용합니다.

| 기능 | 함수명 | 의미 |
| :---- | :---- | :---- |
| 생성 | `create_` | 새로 만들기 |
| 하나 조회 | `get_` | 하나 가져오기 |
| 여러 개 조회 | `list_` | 목록 가져오기 |
| 수정 | `update_` | 변경하기 |
| 삭제 | `delete_` | 삭제하기 |

create\_user()        \# 사용자 생성

get\_user()           \# 사용자 1명 조회

list\_users()         \# 사용자 목록 조회

update\_user()        \# 사용자 수정

delete\_user()        \# 사용자 삭제

---

## 3\. 함수명은 동작 \+ 대상

"무엇을 한다 \+ 무엇을" 순서로 작성합니다.

create\_restaurant()   →  생성 \+ 식당

send\_message()        →  보내기 \+ 메시지

render\_navbar()       →  보여주기 \+ 네비게이션

조건이 필요하면 뒤에 붙입니다.

get\_user\_by\_email()       \# 사용자 \+ 이메일로 조회

get\_restaurant\_by\_id()     \# 식당 \+ ID로 조회

---

## 4\. 자주 사용하는 단어

| 하고 싶은 일 | 사용 |
| :---- | :---- |
| 생성 | `create_` |
| 하나 조회 | `get_` |
| 목록 조회 | `list_` |
| 수정 | `update_` |
| 삭제 | `delete_` |
| 검색 | `search_` |
| 외부 API에서 가져오기 | `fetch_` |
| 화면에 보여주기 | `render_` |
| 버튼 등의 동작 처리 | `handle_` |
| 검사하기 | `validate_` |

예:

search\_restaurants()

fetch\_kakao\_places()

render\_navbar()

handle\_login()

validate\_email()

---

## 5\. 이것만은 피하기 🚨

무슨 일을 하는지 알 수 없는 이름은 사용하지 않습니다.

❌

data()

test()

func1()

process()

abc()

구체적으로 작성합니다.

✅

get\_user()

create\_message()

search\_restaurants()

---

## ⭐ 우리 팀 핵심 규칙

**함수명 \= 동작 \+ 대상**

create \+ user       → create\_user()

get \+ restaurant    → get\_restaurant()

update \+ profile    → update\_profile()

delete \+ message    → delete\_message()

같은 기능은 팀원 모두 같은 단어를 사용합니다. 예를 들어 조회할 때 누구는 `find_user()`, 누구는 `select_user()`라고 하지 않고 **`get_user()`로 통일**합니다.  
